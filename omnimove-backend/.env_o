# ── OmniMove — Local Environment Variables ────────────────────────────────
# Real values for local development. NEVER commit this file.

# ── PostgreSQL ────────────────────────────────────────────────────────────
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/omnimovedb
SPRING_DATASOURCE_USERNAME=omnimove
SPRING_DATASOURCE_PASSWORD=omnimove

# ── Redis ─────────────────────────────────────────────────────────────────
SPRING_REDIS_PASSWORD=omnimove_dev

# ── JWT ───────────────────────────────────────────────────────────────────
JWT_SECRET=omnimove-jwt-secret-change-in-production-must-be-long

# ── InfluxDB ──────────────────────────────────────────────────────────────
INFLUX_URL=http://localhost:8087
INFLUX_TOKEN=lREzpyKsjtCp-b_-1qBt19XLQFp4-T_DfhKOePUPrZUI53QVM0twOikxIVtg2aTvr6ss1wYVVNXrrHtjMRSxiA==

# ── Email (Gmail SMTP) ────────────────────────────────────────────────────
MAIL_USERNAME=omnimove.noreply.demo@gmail.com
MAIL_PASSWORD=wkruqaoocekntwub

# ── CassiTrack connection (must match SSE_API_TOKEN in CassiTrack) ────────
CASSITRACK_URL=http://localhost:8280/cassitrack/api/v1
CASSITRACK_API_TOKEN=JfWIYLF6BednHmUkOliUQbHHpuVIIBzqN/DcpJWsXUM=
CASSITRACK_NETEX_URL=http://localhost:8280/cassitrack/api/static/netex

# ── App base URL (used in email links) ───────────────────────────────────
APP_BASE_URL=http://localhost:8180

# ── CORS ──────────────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:8180,http://localhost:3000

# ── External APIs ─────────────────────────────────────────────────────────
WEATHER_API_KEY=a50cdb6508e8300b5af885e88e9dcb3a
GOOGLE_MAPS_API_KEY=AIzaSyDIl_xVa4mw7Z_skmfyqgMT58m8-PInABQ
# The assistant now speaks the OpenAI dialect; provider defaults to Regolo.
# Empty key = canned fallback answers, no failure.
AI_API_KEY=sk-OqjRVEIdKAy3MWC0xVW8qg

# ── Sign in with Google ───────────────────────────────────────────────────
# OAuth 2.0 Client ID (type "Web application"), project cassitrack.
# Public by design: it travels inside every Google button. The client secret
# that came with it is NOT used by this app and must not be stored anywhere.
# Registered JavaScript origins: http://localhost:8180, https://devaidalab.unicas.it
GOOGLE_OAUTH_CLIENT_ID=477973570842-2uld3aes6n6qbimqlni0h0k9k9dghm7u.apps.googleusercontent.com

# ── reCAPTCHA v2 (checkbox) ───────────────────────────────────────────────
# Registered on google.com/recaptcha/admin for localhost + devaidalab.unicas.it.
# The site key is public (it sits inside the widget); the secret must never
# leave the server or reach the repository. Regenerate it from the console if
# it is ever exposed. On/off is the switch in the admin dashboard.
RECAPTCHA_SITE_KEY=6LdRYpwtAAAAAOq3U3kE98xu3dD-SutUiJyyh0MX
RECAPTCHA_SECRET_KEY=6LdRYpwtAAAAAAx8y1cun-FI0d7DFSOJnnbJpzg3
