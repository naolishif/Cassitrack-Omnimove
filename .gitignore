# Java compiled files
target/
*.class
*.jar

# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Maven
.mvn/wrapper/maven-wrapper.jar

# Environment / secrets — NEVER commit these files
.env
*.env
.env_server
env
**/env
**/.env
cassitrack-backend/env
omnimove-backend/env

# ⚠️  SECURITY NOTICE — Git history cleanup required:
# Secrets were committed in early commits (see omnimove-backend/env, cassitrack-backend/env).
# Run: git filter-repo --path cassitrack-backend/env --path omnimove-backend/env --invert-paths
# Then force-push all branches and rotate ALL secrets exposed (see OWASP_SECURITY_AUDIT.md).

# Docker volumes
mosquitto/data/
mosquitto/log/

# MQTT broker credentials — NEVER commit the real password file.
# Provision it per environment (see mosquitto/config/passwd.example).
# ⚠️  The hashes were tracked in earlier commits: rotate the MQTT passwords and
#     scrub git history (git filter-repo --path cassitrack-backend/mosquitto/config/passwd --invert-paths).
**/mosquitto/config/passwd

# Python
__pycache__/
*.pyc

# OS files
.DS_Store
Thumbs.db
